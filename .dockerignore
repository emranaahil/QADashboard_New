# Dependencies (installed in image)
node_modules
web/node_modules
backend/**/node_modules

# Legacy static frontend — superseded by web/ (Next.js)
frontend

# Next.js build artifacts (rebuilt inside Docker)
web/.next
web/out
web/node_modules

# Dev / IDE
.git
.gitignore
.cursor
.mcps
terminals
*.md
!README.md

# Runtime data (mount volumes in production)
backend/**/jobs
backend/**/reports
backend/keyword-check/storage
backend/shared/data
jobs
reports
storage

# Logs & env secrets
*.log
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
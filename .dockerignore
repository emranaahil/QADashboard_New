# Dependencies (installed in image)
node_modules
web/node_modules
backend/**/node_modules

# Legacy static frontend — superseded by web/ (Next.js)
frontend

# Next.js build artifacts (rebuilt inside Docker)
web/.next
web/out
web/node_modules

# Dev / IDE
.git
.gitignore
.cursor
.mcps
terminals
*.md
!README.md

# Bundled module reports live under backend/ and are seeded into STORAGE_ROOT on Render.
# Do not exclude backend/**/jobs or reports — they must ship in the image for first deploy.

# Logs & env secrets
*.log
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db